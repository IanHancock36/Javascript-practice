// compare the index of 0 to the index of 1 
// if the index of 1 contains the same letters but not All the letters of index 0 
// return true else return false. index of 1 hell compared to index of 0 hello should
// should return true 







// function mutation(array) {
//     firstWord = array[0]
//     secondWord = array[1]
//     array.some(value => value.toLowerCase() === 'hell')
// }
  
//  console.log( mutation(["hello", "hell"]));

const array = ['SAMANTHA', 'sam'];
secondWord = array[1]
console.log(array.some(value => value.toLowerCase() === secondWord))

